import { APIClient, FetchProvider} from '@greymass/eosio'
import fetch from 'node-fetch'
/*
const jungle4 = new APIClient(
    { provider: new FetchProvider('https://jungle4.genereos.io:443', { fetch }) }
)

async function get_info() {
    return await jungle4.v1.chain.get_info()
}

get_info()
    .then((value) => {
        console.log(value)
    })
.catch((error) => {
    console.log(`Failed got an error: ${error.toString()}`)
})

 */